# Charlie Build Environment Variables
# This file contains dummy values for Charlie to run production builds
# in ephemeral devbox environments without real credentials

# Next.js Public Variables (dummy values for build)
NEXT_PUBLIC_SUPABASE_URL=https://dummy.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy-anon-key-for-build-verification-only
NEXT_PUBLIC_API_BASE_URL=http://localhost:3001
NEXT_PUBLIC_APP_URL=http://localhost:3001

# Better Auth Configuration (dummy values)
BETTER_AUTH_SECRET=1234567890abcdef1234567890abcdef1234567890abcdef1234567890abcdef
BETTER_AUTH_URL=http://localhost:3001/api/auth

# Database Configuration (dummy values - build doesn't need real DB)
DATABASE_URL=postgresql://dummy:dummy@localhost:5432/dummy
DB_HOST=localhost
DB_PORT=5432
DB_NAME=dummy
DB_USER=dummy
DATABASE_PASSWORD=dummy

# Supabase Configuration (dummy values)
SUPABASE_SERVICE_ROLE_KEY=dummy-service-role-key-for-build-only

# Node Environment
NODE_ENV=production

# Optional Strava Integration (dummy values)
STRAVA_CLIENT_ID=dummy-strava-id
STRAVA_CLIENT_SECRET=dummy-strava-secret
STRAVA_REDIRECT_URI=http://localhost:3001/api/strava/callback

# Email Service (dummy values)
RESEND_API_KEY=dummy-resend-key

# Note: These are all dummy values safe to commit to version control
# They allow Charlie to verify build integrity without exposing real credentials
# The build process does not require actual database or API connectivity
