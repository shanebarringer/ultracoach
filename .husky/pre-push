echo "🚀 Running pre-push checks..."

# Build check (ensures production build works)
# E2E tests run in CI - no need to duplicate locally
echo "🏗️  Checking build..."
pnpm build || {
  echo "❌ Build failed. Please fix build errors before pushing."
  echo "💡 Tip: Run '/husky' command for full local checks including E2E tests"
  exit 1
}

echo "✅ Pre-push passed! 🎉"
echo "💡 E2E tests will run in CI. Use '/husky' for full local checks."
